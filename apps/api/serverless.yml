component: express
name: shopping-api
app: shopping-system

inputs:
  src: ./
  functionName: ${name}
  region: ap-guangzhou
  runtime: Nodejs14.18
  exclude:
    - .env
  environment:
    NODE_ENV: production
